# 初期管理者ログインID
ADMIN_LOGIN_ID=admin
# 初期管理者パスワード（本番環境では必ず変更すること）
ADMIN_PASSWORD=AdminPass123
# JWT署名キー（本番環境では必ず変更すること）
JWT_SECRET_KEY=change-me-in-production
# Playwright MCPサーバーのポート番号
PLAYWRIGHT_PORT=8931
